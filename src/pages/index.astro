---
import BlogLinkTile from "../components/blog-link-tile.astro";
import DefaultLayout from "../layouts/default-layout.astro";
import { convertGlobMeta, sortBlog } from "../utilities/metadata-util";

const blogs = Object.values(import.meta.glob("./blogs/*.md", {eager: true}))
    .map(convertGlobMeta)
    .sort(sortBlog)
    .toReversed();
---
<DefaultLayout title={"Hallo"} tabName={"neon95"}>
    {(blogs.map(
        b => <BlogLinkTile metadata={b} />
    ))}
</DefaultLayout>